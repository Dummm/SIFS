#include "getattr.h"
#include "tar_structure.h"
#include <string.h>
int o_getattr(const char* path, struct stat* sbuf) {
	//if( strcmp(path, "/") == 0 ) 
		printf("Path: %s\n", path);
	
	return -1;
}

